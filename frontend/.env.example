# Frontend env (Vite) — szablon.
# Pozostaw VITE_API_URL puste: frontend automatycznie wyśle zapytania
# na window.location.hostname:4000, więc telefon w LAN też zadziała.
# Ustaw konkretny URL tylko gdy API jest na innym hoście/porcie:
#   VITE_API_URL=http://192.168.8.100:4000
VITE_API_URL=